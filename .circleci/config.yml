version: '2.1'
jobs:
  tests:
    docker:
      - image: cirrusci/flutter
    steps:
      - checkout
      - run:
          name: Flutter Doctor
          command: flutter doctor
      - run:
          name: Build iOS
          command: flutter test
          working_directory: ./auth0_flutter
workflows:
  build:
    jobs:
      - tests
