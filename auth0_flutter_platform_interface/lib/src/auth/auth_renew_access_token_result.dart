import '../credentials_with_userprofile.dart';

class AuthRenewAccessTokenResult extends CredentialsWithUserProfile {
  AuthRenewAccessTokenResult.fromMap(final Map<dynamic, dynamic> result)
      : super.fromMap(result);
}
